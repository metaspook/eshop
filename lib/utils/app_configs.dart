class AppConfig {
  AppConfig._();
  static const appTitle = 'EShop';
  static const baseUrl = 'https://panel.supplyline.network';
  static const productsPath =
      '$baseUrl/api/product/search-suggestions/?limit=10&offset=10&search=rice';
}
